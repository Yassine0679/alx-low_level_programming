#include "main.h"

/**
 * int _isupper - checks for uppercase character
 * Return: 1 if c is uppercase
 * otherwise returns 0
*/

int _isupper(int c)
{
if (i <= 90 && i >= 65)
{
return (1);
}
else
{
return (0);
}
}

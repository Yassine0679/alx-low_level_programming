#include "main.h"

/**
 * int _isupper - checks for uppercase character
 * Return: 1 if c is uppercase
 * otherwise returns 0
*/

int _isupper(int c)
{
if (i <= 'Z' && i >= 'A')
{
return (1);
}
else
{
return (0);
}
}
